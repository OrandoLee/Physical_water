import { defineConfig } from 'vite'

export default defineConfig({
  // GitHub Pages project path for https://orandolee.github.io/Physical_water/.
  // If deploying to a root domain, Vercel, or Netlify root path, change this to base: '/'.
  base: '/Physical_water/',
})
